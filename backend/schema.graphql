type Page {
  id: ID!
  blocks: [Block]
  creator: User
  createdAt: DateTime!
  updatedAt: DateTime
}

type Block {
  id: ID!
  type: String!
  html: String
  imageUrl: String
}

type User {
  email: String! @id
  name: String!
  pages: [Page] @hasInverse(field: creator)
  createdAt: DateTime!
  updatedAt: DateTime
}